<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <!-- CORRECCIÓN: Título dinámico que cambia según la vista -->
  <title><%= titulo %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <link href="/css/postulaciones.css" rel="stylesheet">
  <link href="/css/navbarStyles.css" rel="stylesheet">
</head>
<body>
  <%- include('navbar') %>

   <div class="container-fluid mt-5 mb-5">
     <div class="main-card">
    <!-- CORRECCIÓN: Título dinámico -->
    <h2 class="mb-4"><%= titulo %></h2>

    <% if (success_msg) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= success_msg %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>
    <% if (error_msg) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error_msg %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <div class="card mb-4">
        <div class="card-body d-flex justify-content-between align-items-center flex-wrap gap-2">
            <!-- CORRECCIÓN: El 'action' del formulario ahora es dinámico -->
            <form action="<%= vistaActivos ? '/expedientes' : '/expedientes/desactivados' %>" method="GET" class="d-flex flex-grow-1" style="min-width: 250px;">
                <input type="text" class="form-control me-2" name="busqueda" placeholder="Buscar por nombre o código..." value="<%= typeof busqueda !== 'undefined' ? busqueda : '' %>">
                <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i></button>
            </form>
            <!-- CORRECCIÓN: Botón para cambiar entre vistas de activos y desactivados -->
            <div>
              <% if (vistaActivos) { %>
                <a href="/expedientes/desactivados" class="btn btn-secondary">
                  <i class="fas fa-archive"></i> Ver Desactivados
                </a>
              <% } else { %>
                <a href="/expedientes" class="btn btn-success">
                  <i class="fas fa-list"></i> Ver Activos
                </a>
              <% } %>
            </div>
        </div>
    </div>

    <div class="table-responsive">
      <table class="table table-striped align-middle">
        <thead>
          <tr>
            <th>Código</th>
            <th>Nombre Completo</th>
            <th>Fecha Nacimiento</th>
            <th>Edad</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% if (ninos && ninos.length > 0) { %>
            <% ninos.forEach(nino => { %>
              <tr>
                <td><%= nino.codigo %></td>
                <td><strong><%= nino.nombre_completo %></strong></td>
                <td><%= nino.fecha_nacimiento ? new Date(nino.fecha_nacimiento).toLocaleDateString() : '' %></td>
                <td><%= nino.edad || '' %></td>
                <td>
                  <a href="/expedientes/<%= nino.id %>" class="btn btn-sm btn-info">
                    <i class="fas fa-eye"></i> Ver
                  </a>
                  <!-- ================================================================== -->
                  <!-- =========== INICIO DE LA CORRECCIÓN: BOTÓN ACTIVAR/DESACTIVAR ========== -->
                  <!-- ================================================================== -->
                  <% if (user && (user.rol === 'Secretaria' || user.rol === 'Directivo')) { %>
                    <form action="/expedientes/<%= nino.id %>/toggle-estado" method="POST" class="d-inline" onsubmit="return confirm('¿Estás seguro de que quieres cambiar el estado de este expediente?');">
                      <!-- El texto y color del botón cambian según si la vista es de activos o desactivados -->
                      <% if (vistaActivos) { %>
                        <button type="submit" class="btn btn-sm btn-warning">
                          <i class="fas fa-toggle-off"></i> Desactivar
                        </button>
                      <% } else { %>
                        <button type="submit" class="btn btn-sm btn-success">
                          <i class="fas fa-toggle-on"></i> Activar
                        </button>
                      <% } %>
                    </form>
                  <% } %>
                  <!-- ================================================================== -->
                  <!-- ============ FIN DE LA CORRECCIÓN: BOTÓN ACTIVAR/DESACTIVAR ============ -->
                  <!-- ================================================================== -->
                </td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr>
              <td colspan="5" class="text-center">No se encontraron expedientes.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
        </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

